import { SecondaryColumnPipe } from './secondary-column.pipe';

describe('SecondaryColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondaryColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
