import { FilteroperationPipe } from './filteroperation.pipe';

describe('FilteroperationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteroperationPipe();
    expect(pipe).toBeTruthy();
  });
});
