import { NonspecialcharDirective } from './nonspecialchar.directive';

describe('NonspecialcharDirective', () => {
  it('should create an instance', () => {
    const directive = new NonspecialcharDirective();
    expect(directive).toBeTruthy();
  });
});
