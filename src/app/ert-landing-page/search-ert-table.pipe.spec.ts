import { SearchErtTablePipe } from './search-ert-table.pipe';

describe('SearchErtTablePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchErtTablePipe();
    expect(pipe).toBeTruthy();
  });
});
