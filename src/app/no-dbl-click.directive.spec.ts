import { NoDblClickDirective } from './no-dbl-click.directive';

describe('NoDblClickDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDblClickDirective();
    expect(directive).toBeTruthy();
  });
});
