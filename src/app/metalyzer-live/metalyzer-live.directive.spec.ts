import { MetalyzerLiveDirective } from './metalyzer-live.directive';

describe('MetalyzerLiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MetalyzerLiveDirective();
    expect(directive).toBeTruthy();
  });
});
