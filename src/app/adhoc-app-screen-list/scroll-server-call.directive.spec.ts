import { ScrollServerCallDirective } from './scroll-server-call.directive';

describe('ScrollServerCallDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollServerCallDirective();
    expect(directive).toBeTruthy();
  });
});
