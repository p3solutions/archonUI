import { NotWildCharDirective } from './not-wild-char.directive';

describe('NotWildCharDirective', () => {
  it('should create an instance', () => {
    const directive = new NotWildCharDirective();
    expect(directive).toBeTruthy();
  });
});
