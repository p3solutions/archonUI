import { AlphabetAllowedDirective } from './alphabet-allowed.directive';

describe('AlphabetAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabetAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
